&nbsp; 넣어쓰기(쓸일은 없음)
<br> 줄바꿈
&copy; 카피라이트 아이콘
제목태그 h1~h5 기본폰트가 bold로 지정 폰트 크기 차이
블록태그 자동으로 줄바꿈이된다(hn)
<p>같은 글자취급의 태그는 인라인 태그
단일태그는 수 가적다(태그끝에 /를 붙이지않는 태그들). hr img tag input..
<b> bold
<u> 밑줄
<i> 글자 기울이기(이탤릭체)

<b><u><i>체포·구속·압수</i></u></b>
태그를 여러개 감싸는것은 표준에 어긋남 
<ol start="6"> 목록의 6째부터 순서가 시작
순번이 있다 ol 1,2,3
순번이 없다 ul ●,●,●
image 확장자 jpg, png, gif, svg
jpg 고화질 사진이미지
png 고화질 사진이미지 ----> 배경 투명화
gif 블릿이미지, 로고 ----> 저해상도 이미지
svg 스케일러블 벡터 그래픽스  
<image 경로 개념>
상대경로 일때 >> src--> 경로 방향, img/ ---> '나 거기로 들어갈꺼야'
절대경로 라는 건 실제 컴퓨터에 있는 파일의 위치/ 서버 컴퓨터로 옮기면 경로가 바뀌어 못 읽음, 하나하나 경로 수정해야함
<img src="/" ----> 상위폴더 작업환경 보임> --url root 폴더 기준으로 경로를 잡음
width="" height=""
< 동영상, 음악 >
1.동영상 format - mov, mp4, avi, mkv, wmv, 
  --> 지원 코덱을 일일이 사용자 컴에 설치 x ,문제는 외부 플러그인을 이용해서 재생(active X, 외부 설치파일)
  html 별도의 코덱이 필요없이 웹브라우저에서 재생 가능하게 바뀜
  webm, ogv, mp4 <---  html5에서 지원하는 동영상 포맷

2.음악 format 
  mp3, ogg, aac, flac...
  mp3, ogg <--- html5에서 지원하는 음원 포맷
  <!--contrls- 동영상 컨트롤러 표시-->
    <!--loop - 반복재생-->
    <!--autoplay 자동재생-// 브라우저 보안 정책에 따라 적용이 안될수도 있음->
    <!--poster -- 동영상 썸네일 이미지 삽입 -->

    제일 중요한건 경로 잡는 것

    문단은 P태그로 묶을것

    하이퍼 링크 태그 a
    속성들
    href 속성 = 이동할 웹페이지 경로, 첨부파일 경로
    target 속성 = 페이지 이동시 출력할 창
    download = 첨부파일 처럼 다운로드 설정 ---> 별로 안중요
    title = 툴팁

    정렬 = alt shift f
    네임앵커는 하이퍼링크 태그에서 아이디 속성값으로 스크롤바를 이동시키는 기능
    href="#id값" 해당하는 아이디 값으로 이동하겠다는 뜻
    아이디 속성값은 중복이 되지 않는다.

    <iframe> 써봤자 동영상 정도에만 쓰임
    <a> 태그 사이에 뭐가 있으면 하이퍼링크 누르면 href 웹으로 이동


     --테이블에 들어가는 태그들 ---
     <table></table> 테이블 영역
     <tr> 테이블의 행 영역 (줄 영역)
     <td> 테이블의 열 영역 (열 영역)-->한칸-->셀
     셀 하나하나 사이의 거리 = sell spacing
     <th> 테이블 제목 
     <caption> 표 제목, 자동으로 가운데 정렬


     <fome> 폼 태그는 사용자로부터 직접적으로 입력을 받아서
            서버로 데이터를 전송을 해주는 태그 ex)로그인

    서버로 전송하는 값 value

    <input> 태그의 속성


    체크박스/ 라디오버튼
